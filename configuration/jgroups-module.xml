<module xmlns="urn:jboss:module:1.3" name="org.jgroups">

    <resources>
        <!-- TODO fix version dependency -->
        <resource-root path="jgroups-4.0.15.Final.jar"/>
    </resources>

    <dependencies>
        <module name="javax.api"/>
        <module name="com.mysql.jdbc"/>

        <!-- the following is not in used but it does not boot without them :( -->
        <module name="org.jgroups.azure" />
        <module name="org.jgroups.kubernetes" />
    </dependencies>
</module>
